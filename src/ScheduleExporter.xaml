<Window x:Class="ScheduleExporter.ExportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Schedule Exporter" 
        Height="870" 
        Width="630"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    
    <Window.Resources>
        <Style TargetType="Label">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="10,5"/>
        </Style>
        <Style TargetType="CheckBox">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </Window.Resources>
    
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <Label Grid.Row="0" 
               Content="Export schedules to Excel file" 
               FontSize="18" 
               FontWeight="Bold"
               Margin="0,0,0,15"/>

        <!-- File Selection Buttons -->
        <Grid Grid.Row="1" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <Button Grid.Column="0" 
                    Content="New File" 
                    Height="40"
                    Click="ButtonNewFile_Click"/>
            <Button Grid.Column="2" 
                    Content="Select File" 
                    Height="40"
                    Click="ButtonSelectFile_Click"/>
        </Grid>

        <!-- File Path -->
        <Grid Grid.Row="2" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <Label Grid.Column="0" 
                   Content="File path" 
                   FontWeight="Bold"
                   Margin="0,0,10,0"/>
            <TextBox Grid.Column="1" 
                     x:Name="textBoxFilePath"
                     VerticalContentAlignment="Center"
                     Height="32"
                     FontSize="14"/>
        </Grid>

        <!-- Schedules Header -->
        <Grid Grid.Row="3" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <Label Grid.Column="0" 
                   Content="Schedules to write" 
                   FontWeight="Bold"/>
            <CheckBox Grid.Column="1" 
                      x:Name="checkBoxSelectAll"
                      Content="Select All"
                      Checked="CheckBoxSelectAll_Changed"
                      Unchecked="CheckBoxSelectAll_Changed"/>
        </Grid>

        <!-- Schedules List -->
        <ListBox Grid.Row="4" 
                 x:Name="listBoxSchedules"
                 Margin="0,0,0,15"
                 ScrollViewer.VerticalScrollBarVisibility="Auto">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <CheckBox Content="{Binding Name}" 
                              IsChecked="{Binding IsChecked}"
                              FontSize="14"
                              Margin="2"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!-- Write as String Option -->
        <Grid Grid.Row="5" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <Label Grid.Column="0" 
                   Content="Write numbers as strings" 
                   FontWeight="Bold"/>
            <CheckBox Grid.Column="1" 
                      x:Name="checkBoxWriteAsString"
                      Content="Yes/No"
                      IsChecked="True"
                      Width="120"
                      HorizontalAlignment="Left"/>
        </Grid>

        <!-- Exclude Empty Rows Option -->
        <Grid Grid.Row="6" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <Label Grid.Column="0" 
                   Content="Exclude all empty rows" 
                   FontWeight="Bold"/>
            <CheckBox Grid.Column="1" 
                      x:Name="checkBoxExcludeEmpty"
                      Content="Yes/No"
                      Width="120"
                      HorizontalAlignment="Left"/>
        </Grid>

        <!-- Write Button -->
        <Button Grid.Row="7" 
                Content="Write" 
                Height="50"
                FontSize="16"
                HorizontalAlignment="Right"
                Width="283"
                Click="ButtonWrite_Click"/>
    </Grid>
</Window>
